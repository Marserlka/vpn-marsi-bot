# Telegram
BOT_TOKEN=123456:your-bot-token-from-botfather
# Comma-separated Telegram IDs with access to /admin
ADMIN_IDS=111111111,222222222

# Database
DB_URL=sqlite+aiosqlite:///./vpn_marsi.db

# AmneziaWG peer-management agent (primary protocol, see TZ 3.2 — the bot
# provisions AmneziaWG peers via this small sidecar service running on the
# VPS, not through Marzban, which has no native WireGuard support).
# Port 443 on purpose — many bot hosts (incl. BotHost) block outbound to
# non-standard ports; the agent listens on 443 for exactly this reason.
AWG_AGENT_BASE_URL=https://your-vps-domain
AWG_AGENT_TOKEN=change-me

# Marzban panel (VLESS-Reality and Shadowsocks, alternative protocols to
# AmneziaWG/WireGuard — see TZ 3.4)
MARZBAN_BASE_URL=https://your-marzban-domain:8000
MARZBAN_ADMIN_USERNAME=admin
MARZBAN_ADMIN_PASSWORD=change-me
# Name of the VLESS-Reality and Shadowsocks inbounds configured in Marzban
# (see panel -> Core -> inbounds)
MARZBAN_INBOUND_TAG=VLESS-Reality
MARZBAN_SS_INBOUND_TAG=Shadowsocks-TCP
# SNI/dest masking domain used by the VLESS-Reality inbound — must be a
# foreign-hosted, TLS1.3 site whose real IPs plausibly include your VPS's
# country
MARZBAN_SNI_MASK=sap.com

# Per-day billing (replaced the old prepaid PLAN_1/2/3 tariffs 2026-09-05,
# see TZ) — each active connection independently costs this per day,
# debited from the owner's balance
PRICE_PER_DAY_RUB=1.5

# One-time free trial (see User.trial_used) — no daily charge for the first
# TRIAL_DAYS days of a trial connection
TRIAL_DAYS=3

# A connection younger than this many days can't be deleted by its owner
MIN_CONNECTION_AGE_DAYS=7

# Referral: flat one-time cash bonus, credited to the referrer's balance the
# first time (and only the first time) their referral tops up their own balance
REFERRAL_BONUS_RUB=10
REFERRAL_CAPTCHA_TIMEOUT_SECONDS=120

SUPPORT_USERNAME=Marserlka

# Legal documents shown before purchase (payment-provider compliance)
PRIVACY_POLICY_URL=https://telegra.ph/Politika-konfidencialnosti-09-03-91
TERMS_URL=https://telegra.ph/Polzovatelskoe-soglashenie-09-03-40
